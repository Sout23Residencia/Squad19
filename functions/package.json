{
  "name": "functions",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "serve": "npx firebase emulators:start --only functions",
    "shell": "npx run build && firebase functions:shell",
    "start": "npx firebase deploy --only functions",
    "logs": "firebase functions:log",
    "login": "npx firebase login",
    "init": "npx firebase init"
  },
  "engines": {
    "node": "18"
  },
  "main": "lib/index.js",
  "dependencies": {
    "docx": "^8.2.3",
    "express": "^4.18.2",
    "file-saver": "^2.0.5",
    "firebase": "^10.5.0",
    "firebase-admin": "^11.8.0",
    "firebase-functions": "^4.3.1",
    "firebase-tools": "^12.7.0"
  },
  "devDependencies": {
    "firebase-functions-test": "^3.1.0",
    "typescript": "^4.9.0"
  },
  "private": true
}
